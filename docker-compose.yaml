services:
  news-site:
    container_name: news-site
    build: .
    env_file:
      - .env
    volumes:
      - ./app:/app:Z
    restart: always
    ports:
      - 3000:3000

  db:
    image: postgres
    restart: always
    env_file:
      - .env
    ports:
      - 5432:5432